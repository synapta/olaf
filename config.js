const JobTypes = [
    { alias: "main", description: "Progetto generico", item_header: "ENTITÀ" }
];

const SourceTypes = [
    { alias: "csv", description: "File CSV locale" }
];

module.exports = {
    JobTypes,
    SourceTypes
};