const JobTypes = [
    { alias: "main", description: "Progetto generico" }
];

const SourceTypes = [
    { alias: "csv", description: "File CSV locale" }
];

module.exports = {
    JobTypes,
    SourceTypes
};